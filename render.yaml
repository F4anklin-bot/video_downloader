services:
  - type: web
    name: franklins
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MAX_FILE_BYTES
        value: "524288000"
